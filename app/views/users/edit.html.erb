<%= render 'form' %>
<%= form_for @user, :html => {:multipart => true} do |f| %> 
	<%= f.file_field :photo %>
	<%= f.hidden_field :photo_cache %>
    <%= image_tag(@user.photo) if @user.photo? %>
  	<%= f.submit %>
<% end %>
